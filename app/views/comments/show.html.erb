<div class="center jumbotron">
<h3>Comment by <%= User.find(@comment.user_id).username %></h3> <%= link_to "Edit", edit_comment_path(@comment), class: "btn btn-primary" %>
  <p>
    <%= @comment.content %>
</div>
